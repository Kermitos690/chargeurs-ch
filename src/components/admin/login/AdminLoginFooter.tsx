
import React from 'react';
import { motion } from 'framer-motion';

const AdminLoginFooter = () => {
  return (
    <motion.div className="mt-4 text-center text-sm text-gray-500">
      <p>Système d'administration réservé au personnel autorisé</p>
    </motion.div>
  );
};

export default AdminLoginFooter;
